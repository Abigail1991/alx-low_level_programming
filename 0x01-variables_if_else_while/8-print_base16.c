#include <stdio.h>

/**
 * main - Entry
 * Return: Always 0
 */
int main(void)
{
	char k;

	int j;

	k = 'a';
	j = 0;
	while
		(j < 10) {
			putchar(j + '0');
			j++;
		}
	while
		(k <= 'f') {
			putchar(k);
			k++;
		}
	putchar('\n');
	return (0);
}

#include <stdio.h>

/**
 * main - Entry
 * Return: Always 0
 */
int main(void)
{
	char f;

	int j;

	f = 'a';
	j = 0;
	while
		(j < 10) {
			putchar(j + '0');
			j++;
		}
	while
		(f <= 'f') {
			putchar(f);
			f++
		}
	putchar('\n');
	return (0);
}

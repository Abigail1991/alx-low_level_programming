#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _atoi - a function that converts strings to integer
 * @s: char
 * Return: output
 */
int _atoi(char *s)
{
	char *s;
	int x = atoi(s)

		printf("converting s ; %d\n", x)
		return (0);
}

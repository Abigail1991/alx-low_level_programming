#include "main.h"

/**
 * _strlen - a function that returns the lenght of a string
 * @s: starting
 * Return: Always length
 */
int _strlen(char *s)
{
	int lenght = 0;

	while (s[length] != '\0')
	{
		length++;
	}
	return (length);
}
